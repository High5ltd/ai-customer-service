name: CI - AI Customer Service
run-name: ${{ github.event.pull_request.title || 'Deployment' }} ${{ github.ref_name }}

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, closed]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  # Image: ghcr.io/<owner>/<repo-name>-backend:<tag> (repo name = tên GitHub repo, lowercase).
  # Frontend (nếu build image sau này): <repo-name>-frontend:<tag>
  # K8s repo — chỉnh theo org/repo thực tế (cần secret DEPLOY_PAT). Frontend app deploy riêng (Vercel).
  K8S_REPO: High5ltd/k8s

jobs:
  changes:
    name: Check for File Changes
    runs-on: [self-hosted, Linux, X64]
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.pull_request.merged == true ||
      (github.event_name == 'pull_request' && github.event.action != 'closed')
    permissions:
      pull-requests: read
      contents: read
    outputs:
      rag: >-
        ${{
          github.event_name == 'workflow_dispatch' ||
          steps.filter.outputs.rag == 'true'
        }}
      ui: >-
        ${{
          github.event_name == 'workflow_dispatch' ||
          steps.filter.outputs.ui == 'true'
        }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Filter paths
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            rag:
              - 'RAG/**'
              - 'tests/**'
              - 'evals/**'
              - 'src/**'
              - 'pyproject.toml'
              - 'poetry.lock'
              - 'RAG/Dockerfile'
              - '.github/workflows/**'
            ui:
              - 'UI/**'
              - 'UI/Dockerfile'
              - '.github/workflows/**'

  secret-scan:
    name: Secret Scanning
    runs-on: [self-hosted, Linux, X64]
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.pull_request.merged == true ||
      (github.event_name == 'pull_request' && github.event.action != 'closed')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: TruffleHog OSS
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.pull_request.merge_commit_sha && format('{0}^', github.event.pull_request.merge_commit_sha) || github.event.pull_request.base.sha || 'HEAD^' }}
          head: ${{ github.event.pull_request.merge_commit_sha || github.event.pull_request.head.sha || 'HEAD' }}
          extra_args: --debug --only-verified

  test-rag:
    name: Check (RAG) — ${{ matrix.task.name }}
    runs-on: [self-hosted, Linux, X64]
    needs: changes
    env:
      # conftest imports RAG.main → Settings(); real key not needed for unit tests.
      OPENAI_API_KEY: sk-ci-placeholder
    if: >-
      (github.event_name == 'workflow_dispatch' ||
        needs.changes.outputs.rag == 'true') &&
      !(github.event_name == 'pull_request' && github.event.action == 'closed')
    strategy:
      fail-fast: false
      matrix:
        task:
          - name: Ruff (lint)
            command: poetry run ruff check RAG/ tests/
          - name: Ruff (format)
            command: poetry run ruff format --check RAG/ tests/
          - name: Unit tests
            command: poetry run pytest tests/unit/ --cov=RAG --cov-report=term-missing -q
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Poetry (venv)
        run: |
          python -m venv .venv
          .venv/bin/pip install --no-cache-dir poetry==1.8.3
          echo "$GITHUB_WORKSPACE/.venv/bin" >> "$GITHUB_PATH"

      - name: Install dependencies
        run: poetry install --no-interaction

      - name: Run ${{ matrix.task.name }}
        run: ${{ matrix.task.command }}

  test-ui:
    name: Check (UI) — ${{ matrix.task.name }}
    runs-on: [self-hosted, Linux, X64]
    needs: changes
    if: >-
      (github.event_name == 'workflow_dispatch' ||
        needs.changes.outputs.ui == 'true') &&
      !(github.event_name == 'pull_request' && github.event.action == 'closed')
    strategy:
      fail-fast: false
      matrix:
        task:
          - name: ESLint
            command: pnpm run lint
          - name: Types (TypeScript)
            command: pnpm exec tsc --noEmit
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install pnpm
        run: corepack enable && corepack prepare pnpm@latest --activate

      - name: Install dependencies
        working-directory: UI
        run: pnpm install --frozen-lockfile

      - name: Run ${{ matrix.task.name }}
        working-directory: UI
        run: ${{ matrix.task.command }}

  # Gate: build-push không phụ thuộc test (PR merge/closed bỏ qua test giống mẫu)
  deploy-gate:
    name: Deploy Gate
    runs-on: [self-hosted, Linux, X64]
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    steps:
      - run: echo "Ready for build/deploy (merge or manual trigger)"

  build-push:
    name: Build and Push Backend (Docker)
    runs-on: [self-hosted, Linux, X64]
    needs: deploy-gate
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    permissions:
      contents: read
      packages: write
    outputs:
      version: ${{ steps.version.outputs.version }}
      image_backend: ${{ steps.version.outputs.image_backend }}
      service_backend: ${{ steps.version.outputs.service_backend }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract version and image names
        id: version
        env:
          OWNER_LC: ${{ github.repository_owner }}
          REPOSITORY: ${{ github.repository }}
        run: |
          SHORT_SHA=$(echo ${{ github.event.pull_request.merge_commit_sha || github.sha }} | cut -c1-7)
          VERSION="staging-${SHORT_SHA}"
          OWNER=$(echo "$OWNER_LC" | tr '[:upper:]' '[:lower:]')
          REPO_NAME=$(echo "$REPOSITORY" | cut -d'/' -f2 | tr '[:upper:]' '[:lower:]')
          BACKEND_IMAGE_NAME="${REPO_NAME}-backend"
          BACKEND_TAG="${{ env.REGISTRY }}/${OWNER}/${BACKEND_IMAGE_NAME}:${VERSION}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "image_backend=${BACKEND_TAG}" >> "$GITHUB_OUTPUT"
          echo "service_backend=${BACKEND_IMAGE_NAME}" >> "$GITHUB_OUTPUT"
          echo "BACKEND_IMAGE_LC=${{ env.REGISTRY }}/${OWNER}/${BACKEND_IMAGE_NAME}" >> "$GITHUB_ENV"
          echo "Version (tag): ${VERSION}"
          echo "Backend image: ${BACKEND_TAG}"
          echo "K8s service key: ${BACKEND_IMAGE_NAME}"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) — backend
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.BACKEND_IMAGE_LC }}
          tags: |
            type=raw,value=${{ steps.version.outputs.version }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: RAG/Dockerfile
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}
          cache-from: type=gha,scope=RAG
          cache-to: type=gha,scope=RAG,mode=max

  trigger-deploy:
    name: Trigger Auto Deploy
    needs: build-push
    runs-on: [self-hosted, Linux, X64]
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)

    steps:
      - name: Trigger deployment — backend (K8s)
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.DEPLOY_PAT }}
          repository: ${{ env.K8S_REPO }}
          event-type: deploy-image
          client-payload: |
            {
              "service": "${{ needs.build-push.outputs.service_backend }}",
              "version": "${{ needs.build-push.outputs.version }}",
              "image": "${{ needs.build-push.outputs.image_backend }}",
              "environment": "staging"
            }

      - name: Summary
        run: |
          echo "### Backend deployment triggered (K8s)" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Property | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|----------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Version / tag | ${{ needs.build-push.outputs.version }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Backend image | ${{ needs.build-push.outputs.image_backend }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Service (payload) | ${{ needs.build-push.outputs.service_backend }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Target | staging (Kubernetes) |" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Frontend is deployed separately (e.g. Vercel), not from this workflow." >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Monitor deployment in the [K8s config repo](https://github.com/${{ env.K8S_REPO }}/actions)." >> "$GITHUB_STEP_SUMMARY"
